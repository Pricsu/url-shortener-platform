spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/linkdb
    username: linkuser
    password: linkpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
app:
  jwt:
    secret: change-this-to-a-long-random-secret-in-production-min-32-chars
    expiration-ms: 86400000

server:
  port: 8082